[project]
name = "architecture-notes"
version = "0.1.0"
description = "Retrieval-Augmented Generation for Vietnamese Data Engineering knowledge with hybrid search, source citations, and a local LLM chatbot."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.7.7",
    "rank-bm25>=0.2.2",
]
