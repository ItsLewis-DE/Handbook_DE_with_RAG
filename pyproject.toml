[project]
name = "architecture-notes"
version = "0.1.0"
description = "Vietnamese Data Engineering handbook with system architecture deep dives, database internals, and a RAG reading assistant."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.7.7",
    "rank-bm25>=0.2.2",
]
