Use case: ui-mockup.
Create a polished, realistic design proposal image for the existing Vietnamese personal Data Engineering handbook website "Behind the Pipeline". The supplied local screenshot is the design reference and edit target: retain the familiar forest-green #17302d header, warm paper #f3f0e8 page, vermilion #ed6840 accent, subtle architectural blueprint lines, editorial serif hero, and stacked reading cards. Focus the redesign on navigation, not the whole visual identity. This is a UI proposal board, not a photo of a device, not perspective. Flat crisp front-on screen render, readable Vietnamese accents, sharp consistent spacing, generously sized text. Landscape image about 1536 x 1024.
COMPOSITION: pale warm-paper presentation canvas. At top a quiet concept label and title, below it a wide full desktop website screenshot showing the proposed navigation OPEN and a small portion of the familiar homepage hero behind. In bottom-right a compact mobile screen crop about 280px wide demonstrating the SAME navigation adapted to mobile; use a fine border, no hardware phone. Keep all key UI fully inside canvas. Below the desktop screenshot put one concise Vietnamese design caption, no extra explanatory paragraphs.
Brand: simple small open-book/path line icon, wordmark "Behind the Pipeline" in cream Manrope-like sans with orange detail, tiny descriptor "DATA ENGINEERING HANDBOOK". Keep a slim orange ticker "BEHIND THE PIPELINE  •  DATA ARCHITECTURE  •  DATABASE INTERNALS". Main header height approximately 72px. Do not include old per-article pills, do not include SYS.ONLINE badge. Navigation is medium-weight clean sans-serif, not extremely heavy display font. Fine borders, subtle radius 12px on opened panels, restrained shadow, small mono section labels, orange selected states. No neon, no purple, no glassmorphism, no unrelated imagery, no device mockups, no fake analytics.
Use these real current articles only: "PostgreSQL", "Indexing", "Shared-disk vs shared-nothing", "Apache Airflow", "Apache Spark". Topic groups may be "DATABASE INTERNALS", "DATA ARCHITECTURE", "ORCHESTRATION", "DISTRIBUTED COMPUTING". Existing homepage hero text visible behind navigation: "Hiểu sâu thế giới" and an orange italic "Data Engineering." Preserve the recognizable book/reading-card composition from the reference.

CONCEPT 01: the recommended stable global library dropdown.
Board title exactly "01 / THƯ VIỆN MỞ RỘNG". Small orange tag "ĐỀ XUẤT".
Desktop header: brand on left; only "Trang chủ", active orange "Thư viện" with upward chevron, and a compact outlined search control "Tìm bài viết" with magnifier on right.
Show the library dropdown anchored just beneath "Thư viện", extending into a spacious cream panel across the right two-thirds of the site, leaving some hero visible on left. Panel top row label "KHÁM PHÁ THƯ VIỆN" on left and close icon on right. The library is grouped visually as a 2x2 grid with thin quiet dividers: DATABASE INTERNALS with "PostgreSQL" and "Indexing"; DATA ARCHITECTURE with "Shared-disk vs shared-nothing"; ORCHESTRATION with "Apache Airflow"; DISTRIBUTED COMPUTING with "Apache Spark". Article links use small arrow icons and ample vertical spacing; PostgreSQL can have a subtle pale-orange hover row. Panel bottom separated by rule has "Xem tất cả bài viết" and right arrow. Do not invent article counts or future content.
Mobile crop: compact green brand bar with search and menu icon. Open full-width cream library sheet with title "Thư viện", a close button, and four topic rows. DATABASE INTERNALS expanded with its two article links; other topic rows collapsed with plus icon. Fit readable content.
Caption exactly "Thêm bài viết. Header vẫn gọn."

